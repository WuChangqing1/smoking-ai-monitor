[pytest]
# 把缓存目录固定在仓库内，避免依赖机器上的全局临时目录（部分受限环境不可写）
cache_dir = .pytest_cache
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    # starlette TestClient 内部的 anyio 别名弃用告警，与本项目代码无关
    ignore::DeprecationWarning:starlette.*
