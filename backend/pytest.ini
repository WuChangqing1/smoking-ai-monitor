[pytest]
# 关闭 pytest 自身的缓存：本项目的测试不需要它，而且部分受限环境无法创建
# .pytest_cache 的临时子目录。需要缓存时可命令行加 -p cacheprovider 覆盖。
addopts = -q --strict-markers -p no:cacheprovider

testpaths = tests

filterwarnings =
    # starlette TestClient 内部的 anyio 别名弃用告警，与本项目代码无关
    ignore::DeprecationWarning:starlette.*
